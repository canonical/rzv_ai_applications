diff --git a/Q08_object_counter/src/main.cpp b/Q08_object_counter/src/main.cpp
index 611ec53..afe2817 100755
--- a/Q08_object_counter/src/main.cpp
+++ b/Q08_object_counter/src/main.cpp
@@ -97,6 +97,7 @@ std::map<std::string, int> input_source_map =
         {"MIPI", 1},
 #endif
         {"USB", 2},
+        {"TEST", 3},
 };
 
 #ifdef V2L
@@ -979,6 +980,7 @@ int8_t R_Main_Process()
             bgra_image = yuyv_image;
             infer_time_ms = total_time;
             int total_count = 0;
+            static int total_detections = 0;
             int items = 0;
 
             mtx.lock();
@@ -1027,7 +1029,13 @@ int8_t R_Main_Process()
                             cv::Scalar(255, 255, 255), font_weight);
                 total_count += (int)it->second;
                 items++;
+                if (total_detections < total_count)
+                {
+                    std::cout << "[INFO] " << std::string(it->first) << " detected.\n";
+                }
+                total_detections = total_count;
             }
+            total_detections = total_count;
             cv::putText(bgra_image, "Total Objects:  " + std::to_string(total_count), cv::Point(DISP_IMAGE_OUTPUT_WIDTH + 20, 150),
                         cv::FONT_HERSHEY_DUPLEX, font_size, cv::Scalar(255, 255, 255), font_weight);
             cv::cvtColor(bgra_image, bgra_image, cv::COLOR_BGR2BGRA);
@@ -1370,7 +1378,14 @@ int32_t main(int32_t argc, char *argv[])
     {
         std::cout << "[INFO] USB CAMERA \n";
         std::string media_port = query_device_status("usb");
-        gstreamer_pipeline = "v4l2src device=" + media_port + " ! videoconvert ! appsink";
+        gstreamer_pipeline = "v4l2src device=" + media_port + " io-mode=dmabuf ! video/x-raw,width=640,height=480 ! videoconvert ! appsink -v";
+    }
+    break;
+    // this case added just for automated tests requiring no camera
+    case 3:
+    {
+        std::cout << "[INFO] TEST IMAGE (/home/ubuntu/test.png)\n";
+        gstreamer_pipeline = "filesrc location=/home/ubuntu/test.png ! pngdec ! imagefreeze ! videoconvert ! video/x-raw,format=BGR,width=640,height=480,framerate=30/1 ! appsink";
     }
     break;
     default:
@@ -1553,4 +1568,4 @@ end_main:
     delete[] drpai_output_buf;
     printf("Application End\n");
     return ret_main;
-}
\ No newline at end of file
+}
